{
  "name": "engram",
  "version": "6.48.0",
  "description": "Persistent memory for Claude Code. v6 BREAKING: per-workstation API tokens replace the shared admin token. Issue a keycard via the dashboard /tokens page after upgrade and paste it via /engram:setup. The operator key (ENGRAM_AUTH_ADMIN_TOKEN) lives ONLY on the server host and MUST NOT be pasted into a workstation.",
  "author": {
    "name": "thebtf"
  },
  "homepage": "https://github.com/thebtf/engram",
  "repository": "https://github.com/thebtf/engram",
  "license": "MIT",
  "userConfig": {
    "server_url": {
      "title": "Server URL",
      "description": "Engram server URL (e.g. http://192.168.1.100:37777)",
      "type": "string",
      "required": true,
      "sensitive": false
    },
    "api_token": {
      "title": "API Token (worker keycard)",
      "description": "Per-workstation API token issued via the engram dashboard /tokens page. NOT the operator/admin token (which lives only on the server host). Generate one via the UI, copy it once, paste it here. Revoke from the same page if the workstation is compromised. After rotation, re-run /engram:setup. (v6 breaking: previous versions accepted the operator token here — that is no longer valid.)",
      "type": "string",
      "required": true,
      "sensitive": true
    },
    "engram_quiet": {
      "title": "Quiet mode (mute automatic hook injection)",
      "description": "When enabled, engram stops PUSHING context into the prompt: no session-start behavioral rules / memories / issues, no pre-tool-use or pre-compact context. Capture/learning hooks still run (the memory keeps crystallizing lessons and recording outcomes), and MCP tools are unaffected — quiet stops engram talking, not learning. Use when injected context is more noise than signal. (Codex ≥0.139 does not forward this to hook children — set \"quiet\": true in ~/.engram/config.json there instead.)",
      "type": "boolean",
      "required": false,
      "sensitive": false
    }
  },
  "mcpServers": "./claude/.mcp.json"
}

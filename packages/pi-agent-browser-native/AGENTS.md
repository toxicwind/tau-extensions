# pi-agent-browser-native

Native `pi` integration of `agent-browser` as a `pi` tool.

## Product-specific guidance

- Do **not** bundle `agent-browser` in this project.
- Assume `agent-browser` is installed separately and available on `PATH`.
- Baseline docs and verification against the current recommended `agent-browser` release. Runtime support uses the stable minimum in `scripts/agent-browser-target.mjs` (currently 0.35.0) with no upper cap and no version-specific compatibility shims.
- Do **not** add backwards-compatibility shims for unsupported upstream versions.
- Keep the integration thin and close to upstream `agent-browser` behavior.
- Agent usability is the product boundary. Do not add wrapper-owned authorization or safety gates around upstream-supported commands, paths, sessions, state, restore, profiles, config, file access, launch arguments, or environment variables. Invoking `agent_browser` authorizes the requested upstream capability; pass it through and let upstream, the browser, and the operating system enforce their own contracts. Wrapper validation is for input shape, process integrity, lifecycle correctness, and truthful evidence—not for deciding what the agent or operator may do.
- The primary UX is the agent invoking the native tool directly, not a slash-command-heavy manual workflow.
- Do **not** overengineer or solve hypothetical problems that do not exist in observed behavior.
- Do **not** add a reusable named "browser recipe" runtime layer on top of native `job` / `qa` / `batch` without new dogfood evidence and an explicit design pass; the current no-adopt decision and revisit criteria live in [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md#no-reusable-recipe-layer-yet) and [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md).
- Thoroughly check official `pi` docs/examples/source behavior before inventing bespoke integration patterns. Prefer an official `pi` mechanism whenever one exists.
- Prioritize the global install path first. Most users will install this extension globally, not as a project-local extension.
- Keep the local extension-side documentation good enough that an agent can use the tool without relying on direct `agent-browser` binary help; when upstream `agent-browser` changes, update the repo-readable command reference, prompt guidance, README/docs, and any relevant tests in the same work.
- For this repository, assume a single operator model: no human and no other agent is making changes here besides you.
- Treat every lingering scratch file, temp artifact, browser session, tmux session, or other side effect related to this repository as your responsibility to clean up.

## Documentation placement

- Put user-facing product docs in `README.md` and `docs/`.
- Put agent-specific operational notes, workflows, and testing procedures in this `AGENTS.md`.
- Write documents as complete documents, not iterative logs, unless the document is explicitly meant to be iterative such as `CHANGELOG.md`.

## Upstream capability baseline and command reference

When upstream `agent-browser` changes version or help text, keep three layers aligned:

1. **Canonical metadata** — `scripts/agent-browser-target.mjs` defines the recommended upstream version and minimum supported stable version, while `scripts/agent-browser-capability-baseline.mjs` defines which `agent-browser <args>` help outputs are sampled for drift checks, and which literal tokens must appear in upstream help and in human-written sections of `docs/COMMAND_REFERENCE.md` (grouped as inventory sections). Nothing in this file shells out to `agent-browser`; rebaselining is an explicit maintainer edit after comparing real `--help` output from the installed binary.

2. **Human command guide** — `docs/COMMAND_REFERENCE.md` holds the readable workflows, examples, and constraints. Outside the two HTML-comment bounded generated regions (`upstream-baseline`, `capability-token-baseline`), edits are normal prose. Every human-authored token listed in the baseline must appear somewhere in the doc body so agents searching the repo see real usage, not only generated lists.

3. **Generated blocks** — The baseline checker renders versioned boilerplate into `docs/COMMAND_REFERENCE.md`. After changing the baseline metadata, run `npm run docs -- command-reference write`. Do not hand-edit inside the `<!-- agent-browser-capability-baseline:start ... -->` / `<!-- ...:end ... -->` markers.

Release-readiness checklist: [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) maps each baseline inventory section to human docs, wrapper/runtime behavior, tests, and verification gates. Refresh it when rebaselining upstream.

Verification stack:

- `npm run typecheck` — runs the same `tsc --noEmit` step as the default verify gate (`scripts/project.mjs` `verify typecheck`) without playbook drift, unit tests, or live upstream command-reference sampling; use for fast TypeScript iteration.
- `npm run docs` (or `npm run docs -- command-reference check`) — generated blocks match `agent-browser-capability-baseline.mjs`.
- `npm run verify -- command-reference` — the above plus live `agent-browser --version` and help sampling against the same baseline (requires the targeted upstream on `PATH`).
- Unit coverage for the verifier and block renderer lives in `test/verify-command-reference.test.ts`.

**npm verification facade:** `npm run verify` and `npm run docs` delegate to [`scripts/project.mjs`](scripts/project.mjs) (`verifySteps` / `docsSteps`). The default gate runs generated playbook drift, a clean `npm run build` equivalent for compiled `dist/`, `tsc --noEmit`, `tsx --test --test-concurrency=1 test/**/*.test.ts`, command-reference generated-block checks, then live `scripts/verify-command-reference.mjs` (needs the baseline-matching `agent-browser` on `PATH`). `pre-pr` concatenates the default steps with package-content verification for larger local handoffs without lifecycle/platform/live dogfood cost. `release` concatenates the default steps with the configured-source lifecycle harness, `verify-package.mjs --smoke-pi`, then the Crabbox platform smoke doctor and full macOS/Ubuntu/native-Windows matrix. The packaged Pi smoke installs only the extracted package's runtime dependencies (`--omit=dev --omit=peer --ignore-scripts`) before loading its compiled entrypoint; it must not rely on the maintainer's transitive packages. `prepare` runs `scripts/prepare.mjs`, which installs dev dependencies with lifecycle scripts disabled only when a GitHub/source install omitted the compiler/peer type packages, then builds the compiled `dist/extensions/agent-browser/index.js` entrypoint used by packed and source installs. `npm run build` deletes stale `dist/` before emitting. The default and platform-target facades run it directly; startup-profile and lifecycle build in their focused scripts; package modes rely on npm `prepare`, so every `dist/` consumer still has one guaranteed build owner without a duplicate `prepack` build. `package.json` `prepublishOnly` runs `npm run verify -- release` and then `npm pack --dry-run` during `npm publish` (unless publish uses `--ignore-scripts`). [`test/project-verify.test.ts`](test/project-verify.test.ts) locks default, `pre-pr`, `release`, `startup-profile`, `real-upstream`, `dogfood`, `platform-target`, `platform-smoke`, `package-pi`, and combined-docs orchestration so those steps cannot be dropped silently. `npm run verify -- startup-profile --samples 3` (or the direct script) is safe opt-in entrypoint startup evidence for package-load changes; it must not launch Pi, tmux, mise, npm, browsers, or `agent-browser`. `npm run verify -- dogfood` clean-builds `dist/`, then runs `scripts/verify-agent-browser-dogfood.ts` against the real `agent-browser` on `PATH` and a deterministic loopback HTTP fixture; it complements but does not replace interactive tmux dogfood on real sites. Run `npm run verify -- --help` for opt-in modes and supported passthrough flags.

**Crabbox platform smoke:** [`docs/platform-smoke.md`](docs/platform-smoke.md) is the source of truth for the required matrix, per-target suites (`platform-build`, `browser-dogfood-smoke`), artifact contracts, and provider lease cleanup. The release-blocking checklist is `npm run check:platform-smoke`, `npm run smoke:platform:ubuntu-image`, `npm run smoke:platform:doctor`, then `npm run smoke:platform:all` (the full matrix is also embedded in `npm run verify -- release`). It must pass on `macos`, `ubuntu`, and `windows-native`; missing setup is not a skip. The owner-approved local macOS and hosted native-Windows alternatives for 0.6.12 are documented under [alternate native transports](docs/platform-smoke.md#alternate-native-transports); label their evidence separately from Crabbox SSH/Parallels.

Release-oriented notes also live in [`docs/RELEASE.md`](docs/RELEASE.md) under pre-release and real-upstream sections.

**Opt-in real upstream contract:** `npm run verify -- real-upstream` runs the sessionless plugin probe first, then the broad browser contract in a separate fail-fast `--test-force-exit` process, both from `test/agent-browser.real-upstream-contract.test.ts` with `PI_AGENT_BROWSER_REAL_UPSTREAM=1` against the real `agent-browser` on `PATH` (must meet the stable floor in `scripts/agent-browser-target.mjs`; the output-shape fixture remains aligned to the current capability baseline). Besides inspection, skills, `open`/`eval --stdin`/`snapshot -i`/`batch`, managed-session continuity, nested batch-attachment blocking, cross-harness cookie/localStorage/sessionStorage restore after close, and POSIX symlinked-restore-storage fail-closed behavior, the suite exercises a representative core command matrix on localhost fixtures (`test/helpers/agent-browser-harness.ts` contract pages), plus WebMCP list/invoke/result/cancel and `--no-webmcp` on the 0.37.0 target, native positive/absent navigation availability and first-load header inheritance/clearing, `pushstate`, `vitals`, network route/requests/HAR, diff snapshot/screenshot/url, trace/profiler, console/errors/highlight, stream enable/status/disable, `cookies set --curl`, a `react tree` missing-renderer case, and `wait --download` with explicit assertions around upstream save-path reporting versus on-disk presence. Browser-opening or credential-dependent families such as `inspect`, `dashboard`, `chat`, provider clouds, and OS clipboard flows stay in fake-upstream or manual validation unless a safe deterministic fixture is added; see [`docs/RELEASE.md`](docs/RELEASE.md#real-upstream-contract-validation) for the async download caveat, temp-directory isolation, output-shape fixture maintenance, and common failure triage (including when to refresh `test/fixtures/agent-browser-real-output-shapes.json`).

### Tool result categories

`agent_browser` results include stable machine-readable fields on `details`: `resultCategory` (`success` | `failure`), plus `successCategory` or `failureCategory` with small fixed enums. The human-facing contract and field list live in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details).

- **Module map (maintainers):** `extensions/agent-browser/lib/session-page-state.ts` owns ordered per-session tab target, `refSnapshot` / invalidation, and pinning state driven from `extensions/agent-browser/index.ts`; `extensions/agent-browser/lib/recording-reservations.ts` owns the unbounded transcript-backed namespace/session reservation index and replayed terminal tombstones kept separate from the bounded artifact manifest; `extensions/agent-browser/lib/batch-lifecycle.ts` owns the shared ordered successful-close fold used by live state and transcript replay; a nested close invalidates the pre-close page target so a lifecycle-proven relaunch at `about:blank` is not treated as stale focus drift; the fold evaluates later failed rows too, persists their bounded `lifecycle.effectiveLaunch.browserLaunched` evidence in `batchSteps`, keeps the close terminal only when that evidence is explicitly false, and otherwise conservatively retains active ownership even on the first managed call; every record subcommand with explicit browser-launch evidence (including `record stop`) reactivates the session, every successful close clears wrapper trace/profiler ownership before later successful rows can rebuild it, namespace-scoped `close --all` clears every matching owned/attached/page/ref/route/trace/recording identity, and batch preflight rejects `record start` / `record restart` after close (see `test/agent-browser.extension-validation.test.ts`, `test/agent-browser.runtime.test.ts`, and `test/agent-browser.real-upstream-contract.test.ts`). Top-level structured modes live under `extensions/agent-browser/lib/input-modes/` (`params.ts`, `script.ts`, `job.ts`, `semantic-action.ts`, `lookups.ts`, `electron.ts`, and `types.ts`); script result/lease glue lives in `extensions/agent-browser/lib/orchestration/script-mode.ts`, its compiled child entry is `extensions/agent-browser/script-worker.ts`, and `extensions/agent-browser/lib/process-environment.ts` owns async-local ambient environment isolation across its helpers/cleanup. Input resolution/validation lives in `extensions/agent-browser/lib/orchestration/input-plan.ts`; managed restore orchestration/sticky state lives in `managed-session-restore.ts`, page-target verification in `page-target-validation.ts`, restore-key and storage/project identity in `managed-session-storage.ts`, snapshot ownership retention in `managed-session-snapshots.ts`, and cross-process coordination in `managed-session-policy-lock.ts`; browser command execution phases live in `extensions/agent-browser/lib/orchestration/browser-run/` (`managed-session-daemon-policy.ts` owns daemon inspection/reuse/close coordination), `extensions/agent-browser/lib/orchestration/output-file.ts` owns explicit result writes and may rehydrate compacted direct/batch-row/whole-batch data only from matching live manifest spills, `results/presentation/common.ts` owns bounded upstream lifecycle extraction, wrapper-side Electron host actions live in `extensions/agent-browser/lib/orchestration/electron-host/`. Keep future execution-phase helpers under `lib/orchestration/`, prompt/TUI/tool-result rendering in `lib/pi-tool-rendering.ts`, and shell-command safeguards in `lib/bash-guard.ts` rather than growing `index.ts`. Under `extensions/agent-browser/lib/results/`, edit behavior in the focused file (`contracts.ts`, `categories.ts`, `action-recommendations.ts`, `next-actions.ts`, `recovery-actions.ts`, `recovery-next-actions.ts`, `selector-recovery.ts`, `network.ts`, `artifact-manifest.ts`, `snapshot.ts` plus `snapshot-*` helpers, `editable-ref-evidence.ts`, `text.ts`, `presentation.ts`, and focused `presentation/*` helpers).
- **Pi transcript alignment:** Pi’s custom-tool path normally marks a tool row failed only when `execute` throws, so a wrapper that returns success while setting `details.resultCategory: "failure"` (for example after `qa` diagnostic reclassification) would otherwise look like a passing tool in JSONL/UI. The extension registers `pi.on("tool_result", …)` and `buildAgentBrowserToolResultPatch` in `extensions/agent-browser/lib/pi-tool-rendering.ts` patches matching `agent_browser` results to set `isError: true` when `event.isError` was not already true. For prose results it also appends the visible `Result category: failure; …; Pi tool isError: true.` line; for caller-requested `--json` results it must preserve the parseable JSON string and patch only `isError`. When changing that behavior, update [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md), release notes if transcript review guidance shifts, and pipeline / extension-validation expectations that assert both prose notice text and JSON preservation (`test/agent-browser.pi-pipeline.test.ts`, `test/agent-browser.extension-validation.test.ts`).
- **Package config and optional web search:** `extensions/agent-browser/lib/config-policy.js` is the canonical policy surface for Pi-scoped package config paths, provider descriptors, env names, config keys, credential source parsing, layer validation/merge, status projection, and redacted summaries. It is `// @ts-check` JSDoc-typed and included in `tsconfig.json`; do not add a hand-written `.d.ts` beside it. `extensions/agent-browser/lib/config.ts` loads config layers and resolves lazy secrets using that shared policy; `scripts/config.mjs` is thin CLI parsing plus file mutation around the same policy. `webSearch.exaApiKey` / `webSearch.braveApiKey` sources use Pi-style literal / `$ENV_VAR` / `!command` semantics from any loaded config layer. `webSearch.enabled: false` disables the companion tool even when environment keys are present, `webSearch.preferredProvider` chooses the default when both Exa and Brave keys resolve (default: Exa), and `webSearch.defaultSearchType` supplies the Exa type only when a call omits `searchType`. Under Pi 0.84.0+, globally installed or CLI-loaded copies load project-local `.pi/config/pi-agent-browser-native/config.json` when Pi trust allows that project layer and skip it when Pi reports the project is untrusted or when launched with `--no-approve`; global config and explicit `PI_AGENT_BROWSER_CONFIG` overrides remain available either way. Browser guidance config currently includes `browser.defaultProfile` and `browser.executablePath`; loaded values are advisory prompt guidance only and are not auto-injected into every launch. `extensions/agent-browser/lib/web-search.ts` owns the optional provider-backed `agent_browser_web_search` tool, bounded Exa-only filters, the fixed primary/version/date/distinct-result Exa prompt, exact normalized-URL first-wins dedupe without overfetch, provider page-date labels, and provider-specific request/normalization behavior behind `WebSearchProviderAdapter` entries. `extensions/agent-browser/index.ts` makes the search tool available when a usable credential source exists from startup or trusted session config and adds browser default prompt guidance from loaded config. Changing config paths, precedence, credential syntax, browser guidance fields, provider choice, redaction, conditional registration, or search result details requires updating README, [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#optional-companion-web-search), [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md#optional-package-config-and-companion-web-search), [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md), [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0121` / `RQ-0122`, package bin/files expectations, and `test/agent-browser.config.test.ts`, `test/agent-browser.web-search.test.ts`, `test/agent-browser.config-cli.test.ts`, plus package smoke expectations when tool registration rules change.
- **Source of truth for enums and classifiers:** `extensions/agent-browser/lib/results/categories.ts` (`classifyAgentBrowserSuccessCategory`, `classifyAgentBrowserFailureCategory`, `buildAgentBrowserResultCategoryDetails`). The tool entrypoint merges category details in `extensions/agent-browser/index.ts`; presentation-layer failures also attach categories from `extensions/agent-browser/lib/results/presentation.ts`.
- **Structured follow-ups:** optional `details.nextActions` (and per-step `batchSteps[].nextActions`) are built by `buildAgentBrowserNextActions` in `extensions/agent-browser/lib/results/action-recommendations.ts`, then merged into Pi-facing `details` from `extensions/agent-browser/index.ts` and `extensions/agent-browser/lib/results/presentation.ts`. See [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details) for the field contract.
- **Page-change summaries:** optional `details.pageChangeSummary` (and per-step `batchSteps[].pageChangeSummary` when a step carries one) compress navigation, mutation, confirmation, and artifact signals into a small struct with `changeType`, `summary`, optional `title`/`url`, artifact fields, and `nextActionIds` that line up with `nextActions`. Types live in `extensions/agent-browser/lib/results/contracts.ts` (`AgentBrowserPageChangeSummary`); assembly lives in `extensions/agent-browser/lib/results/presentation/navigation.ts` (`buildPageChangeSummary`), while explicit command capabilities are centralized in `extensions/agent-browser/lib/command-taxonomy.ts`; successful paths merge from `extensions/agent-browser/index.ts` alongside presentation output.
- **Artifact verification:** optional `details.artifactVerification` and per-step `batchSteps[].artifactVerification` summarize wrapper-checked paths and filtered session-manifest rows (non-`explicit-path` spills) so jobs can branch on counts and per-row `state` without re-statting files. Types live in `extensions/agent-browser/lib/results/contracts.ts` (`ArtifactVerificationSummary`, `ArtifactVerificationEntry`); assembly and `classifyPresentationSuccessCategory` live in `extensions/agent-browser/lib/results/presentation/artifacts.ts` (`buildArtifactVerificationSummary` and helpers). The human field contract is in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details).
- **Presentation redaction:** successful tool and batch-step `details.data` pass through `redactPresentationData` in `presentation/diagnostics.ts`: cookies, passwords, authorization headers, bearer tokens, and similar credentials remain redacted, but wrapper-managed session names, restore identifiers, state paths, and list rows remain visible and usable. `redactInvocationArgs` in `runtime.ts` masks direct credential-bearing arguments such as `--body`, `--headers`, `--password`, proxy credentials, cookies, storage writes, bearer/basic text, SAMLRequest/SAMLResponse/RelayState, and auth-context-only state/nonce. Snapshot spills pass through `redactSensitiveValue` before persistence; whole-batch aggregate spills reapply each row's command-specific data and argv redactors; exact internal page-target URLs remain untouched. Do not treat ordinary upstream resource identifiers or paths as secrets. Changing this behavior requires updating [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), README agent rules, [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md), and presentation/runtime tests.
- **Regression tests:** `test/agent-browser.results.test.ts` locks classifier and next-action behavior; `test/agent-browser.extension-passthrough-validation.test.ts` asserts deterministic fake-upstream `--json` session ordering (explicit `--session connector connect 9222`; implicit managed sessions for `download` with a selector and path, `get url`, `snapshot --compact`, `tab new` / `tab t1` / `tab close`), non-core command families, and provider/skills workflows. The slower real-browser contract in `test/agent-browser.real-upstream-contract.test.ts` covers snapshot-ref native selection, direct-read lifecycle/output writes, and stable-id/label tab switching plus live active-target recovery after selection and `tab close`. `test/agent-browser.presentation.test.ts` covers batch and presentation wiring; `test/agent-browser.extension-tabs.test.ts` covers routine and post-close tab-target state, snapshot spill persistence, and related presentation paths; `test/agent-browser.extension-tab-recovery.test.ts` covers profiled-open tab correction, drift-safe pinned follow-ups, `about:blank` mismatch recovery (including explicit-session ordering), and overlapping explicit-session CDP targets—see `RQ-0097` / `RQ-0100` in [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) when changing those recovery `nextAction` ids or semantics.
- **Native browser/recording evidence:** `results/recording.ts` parses native capture/encoder receipts with explicit unknowns; `orchestration/browser-run/recording-recovery.ts` performs one bounded identity-matched status lookup and preserves original attempts. Keep recording IDs/start windows in the existing reservation journal, never a second ledger or ffprobe runtime. `read-confirmation.ts` plus ordered `SessionPageState` transcript markers preserve native session routing; only native explicit-URL provenance with `capabilities.readRequiresConfirmation: true` skips confirmation page helpers. Legacy routing stays correct without that exemption, and script/explicit identity boundaries still apply. Update the focused browser-evidence/read-confirmation/recording-receipt/recovery tests and contract/architecture/support docs together.
- **When changing artifact verification:** extend `ArtifactVerificationEntry` / `ArtifactVerificationSummary` and `buildArtifactVerificationSummary` together, refresh `test/agent-browser.presentation.test.ts` and `test/agent-browser.extension-validation.test.ts` expectations, and align [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details).
- **When changing categories:** extend the TypeScript unions and classifier in `extensions/agent-browser/lib/results/categories.ts` (and `contracts.ts` when the enum surface changes), update the prose list in `docs/TOOL_CONTRACT.md`, add or adjust tests above, and refresh docs that mention failure-category coverage if the taxonomy changes.
- **When changing next-action recommendations:** update `buildAgentBrowserNextActions` and its unit coverage in `test/agent-browser.results.test.ts`, keep presentation and extension-validation expectations in sync, and align the human contract in `docs/TOOL_CONTRACT.md`. Semantic stale-ref retries and selector-not-found `try-*-candidate` entries appended in `extensions/agent-browser/index.ts`, plus current-snapshot `try-current-visible-ref*` / rich-input recovery entries defined in `extensions/agent-browser/lib/results/selector-recovery.ts`, follow the same contract and extension-validation expectations. Post-click overlay diagnostics (`inspect-overlay-state`, `try-overlay-blocker-candidate-*`, `details.overlayBlockers`, and `Possible overlay blockers` text) are also merged from `extensions/agent-browser/index.ts` (`collectOverlayBlockerDiagnostic`, `buildOverlayBlockerNextActions`); changing detection thresholds, candidate limits, URL gating, or append order requires updating `docs/TOOL_CONTRACT.md`, README and `docs/COMMAND_REFERENCE.md` pitfalls, `docs/SUPPORT_MATRIX.md` `RQ-0073`, playbook guidance when operator copy should shift, and `test/agent-browser.extension-errors-artifacts.test.ts` (`agentBrowserExtension surfaces likely overlay blockers after a no-op click`).
- **Post-`get text` selector visibility (`RQ-0074`):** after successful `get text` on a non-ref CSS selector, `extensions/agent-browser/index.ts` may run `collectSelectorTextVisibilityDiagnostics` / `collectSelectorTextVisibilityDiagnosticForSelector` (extra read-only `eval --stdin` probe), merge `details.selectorTextVisibility` / `selectorTextVisibilityAll`, prepend `Selector text visibility warning` lines, and append `inspect-visible-text-candidates*` next actions (`buildSelectorTextVisibilityNextActions`). Changing probe heuristics, hazard thresholds, selector skip rules (`@e…`, `selectorMayExposeSensitiveLiteral`), sorting, visible preview redaction, or next-action ids requires updating [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), README and [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) extraction notes, [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0074`, playbook guidance when operator copy should shift (`npm run docs -- playbook write`), and `test/agent-browser.extension-errors-artifacts.test.ts` (`agentBrowserExtension warns when get text may read hidden selector matches`).
- **Getter/eval discoverability (`RQ-0078`):** `extensions/agent-browser/lib/results/presentation/errors.ts` owns unknown getter shortcut guidance (`UNKNOWN_COMMAND_SUGGESTIONS`, exact `use-get-title` / `use-get-url` next actions), while `extensions/agent-browser/index.ts` owns the function-shaped `eval --stdin` empty-object heuristic (`getEvalStdinHint`, `details.evalStdinHint`, visible `Eval stdin hint`). Changing shortcut coverage, hint text, next-action ids, or eval-shape heuristics requires updating [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), README and [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) extraction notes, [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0078`, playbook guidance (`npm run docs -- playbook write`), and presentation / extension error-artifact tests.
- **Artifact lifecycle cleanup (`RQ-0079`):** explicit saved artifacts stay host-owned; `extensions/agent-browser/lib/orchestration/browser-run/diagnostics.ts` only reports cleanup guidance on successful close commands (`close`, `quit`, or `exit`) when the artifact manifest contains at least one existing `explicit-path` artifact via `getArtifactCleanupGuidance` (`details.artifactCleanup`, visible `Artifact lifecycle` text, and `explicitArtifactPaths` from `explicit-path` manifest rows only, capped at ten). Do not add browser-tool deletion for arbitrary user paths without a separate design pass. Changing cleanup ownership, path caps, summary text, or close behavior requires updating [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), README, [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md), [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0079`, and `test/agent-browser.extension-errors-artifacts.test.ts` (`agentBrowserExtension reports artifact lifecycle guidance on close`).
- **Timeout partial progress (`RQ-0076`):** when `runAgentBrowserProcess` sets `timedOut`, `extensions/agent-browser/index.ts` runs `collectTimeoutPartialProgress` (planned steps from `compiledJob` for top-level `job`/`qa`, else `getUpstreamEffectiveBatchSteps` from the prepared command tokens and stdin, preserving raw-argument precedence over caller-provided or wrapper-generated stdin; artifact `stat` targets from `getTimeoutStepArtifactPath`; session `get url`, then non-file `get title`, with planned-URL fallback) and merges `details.timeoutPartialProgress` plus visible `Timeout partial progress` from `formatTimeoutPartialProgressText` (URL/path redaction via `sanitizeCurrentPageUrlForTimeoutDiagnostic`, `redactSensitivePathSegmentsForDiagnostic`, `redactInvocationArgs`). When the timed-out target is unknown, final-result assembly removes standalone snapshots and emits `verify-page-target-after-timeout`, a session-scoped fail-fast `get url` then `snapshot -i`; dialog status/accept/dismiss remains valid under the unknown-target guard. Changing step discovery, artifact path rules, redaction, recovery actions, summary copy, or append order requires updating [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) wrapper timeout note, [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0076`, README constrained-job copy, and `test/agent-browser.extension-errors-artifacts.test.ts` (`agentBrowserExtension reports partial progress and artifacts after job timeout`).
- **Managed-session outcome (`RQ-0077`):** after managed-session process execution, `extensions/agent-browser/index.ts` derives `details.managedSessionOutcome` with `buildManagedSessionOutcome`; `formatManagedSessionOutcomeText` appends visible `Managed session outcome: …` when `sessionMode` is `"fresh"` and the outcome’s `succeeded` is false (including after **`qa`** reclassification updates `succeeded`), and when an otherwise-successful replacement records `replacedSessionClosed: false`. Successful explicit `--session <current-wrapper-managed-session> close` also emits a close outcome so live and restored state retire that wrapper-owned session. Changing status names, preservation/abandonment rules, visible copy, or state-transition timing requires updating [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) session-mode notes, [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0077`, README session copy, and `test/agent-browser.extension-errors-artifacts.test.ts` (`agentBrowserExtension reports managed-session outcomes after failed fresh launches`) plus the managed-session assertions in `test/agent-browser.extension-input-modes.test.ts` (`agentBrowserExtension compiles lightweight QA presets and fails diagnostics`) and restore/explicit-close assertions in `test/agent-browser.resume-state.test.ts` / `test/agent-browser.runtime.test.ts`.
- **When changing page-change summaries:** extend `AgentBrowserPageChangeSummary` and `buildPageChangeSummary` together, adjust command taxonomy predicates only when the upstream command surface changes, update `test/agent-browser.presentation.test.ts` and any `pageChangeSummary` assertions in `test/agent-browser.extension-validation.test.ts`, and align `docs/TOOL_CONTRACT.md`.
- **Recording compatibility:** `getRecordCommandOperands` in `command-taxonomy.ts` reads path/optional URL around complete numeric `--fps` pairs without changing argv. Artifact paths, target pinning, and restart ref policy share it; `--fps` is command-local, never an outer global. Starts still conservatively invalidate refs for older supported natives, including failed attempts and start-then-ref batches; the warning and structured summary must not claim an observed page change. `test/agent-browser.batch-fidelity.test.ts` includes opt-in native FPS path/pinning/ref checks with explicit 12-second captures for the observed short/cold Ubuntu recording limitation; do not hide that limit with production waits or weaker assertions.
- **Page-scoped ref preflight (`@e…`):** `extensions/agent-browser/index.ts` records `details.refSnapshot` from successful `snapshot` results (and from the last `snapshot` step inside a successful `batch`), restores per-session snapshots from the active persisted transcript branch on `session_start` and Pi `session_tree`, clears them on successful close commands (`close`, `quit`, or `exit`) and `electron.cleanup` managed-session close steps, rejects stale selector refs in upstream `@eN`, `eN`, and `ref=eN` spellings before spawn when the active tab URL diverges from the snapshot URL, when a ref id is missing from that snapshot, or when a `batch` step would use `@e…` on a guarded command after an earlier invalidating step without a later `snapshot` step in the same plan to clear the latch; batch steps come from `getUpstreamEffectiveBatchSteps` (`lib/orchestration/batch-stdin.ts`), which mirrors upstream's exclusive raw-argument-else-stdin selection with exact-`--bail`-only filtering, so raw-argv batch refs are guarded and stdin refs are not falsely rejected when raw arguments exist; ref scanning follows actual selector slots, not literal fill/select/file operands or keyboard/mouse data; unsupported `--bail=<value>` returns exact-flag guidance without dispatching ignored stdin; tab recovery (verified native selection before semantic/ref helpers under the existing session queue, without rewriting caller argv/stdin or forcing `--bail`; local commands and explicit connect/state-load recovery bypass the old target, unlike content/history actions), artifact/recording preflight (`getArtifactCommandSteps`), batch screenshot preparation (parent directories are created for effective raw rows without rewriting raw strings; absolute-path normalization stays stdin-only), and stale-ref echo args consume the same selection so upstream-ignored stdin cannot be dispatched, fail preflights, or create directories, while the pre-spawn state-policy validator keeps unioning parseable stdin alongside argv as a fail-closed content superset (exact-`--bail` raw-token filtering; stdin parse failures are fatal only when upstream would read stdin). `fill @e…` remains guarded but does not set that batch invalidation latch, so same-snapshot form fills can precede a click/submit row in one batch. Direct or batched WebMCP results that remain `pending`, plus failed `result` / `cancel` attempts while that target is unknown, keep the tab target unknown, suppress same-call snapshot state, and replace standalone snapshot recommendations with `verify-page-target-after-pending-webmcp` (`get url`); `result` / `cancel` and explicit navigation remain available, while a completed WebMCP `batch --bail` must put `get url` before `snapshot -i` to rebuild target/ref state. The wrapper prefixes `refresh-interactive-refs` with `--session` when the call names a session (including upstream-classified `stale-ref` outcomes). Wrapper-owned browser commands are serialized with `session_tree` restore. Caller-owned explicit-session calls use process-local queues keyed by effective canonical namespace/session across helpers (explicit namespace argv wins over inherited `AGENT_BROWSER_NAMESPACE`, including explicit empty default) and main execution, while different identities remain concurrent; namespace-scoped `close --all` is the exception and takes an exclusive barrier across managed plus matching caller-owned queues before it mutates global namespace state; completions also use a branch-state generation guard so stale results cannot overwrite newer branch-visible state. Changing invalidation/guard command sets, messages, merge rules, resume replay, branch rehydration, or state serialization requires updating [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), README pitfalls, [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) ref workflow notes, [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) session policy, [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) RQ-0072/RQ-0087 notes, playbook strings when prompt guidance shifts (`npm run docs -- playbook write`), and the page-scoped ref tests in `test/agent-browser.extension-ref-guards.test.ts` plus No-active-page invalidation coverage in `test/agent-browser.extension-validation.test.ts`.

### One-shot `script` code mode (top-level tool input)

- **Boundary:** `extensions/agent-browser/lib/input-modes/script.ts` owns source validation, child launch/IPC quotas, inner-call policy, abort/kill behavior, and the compiled child path resolver. `extensions/agent-browser/script-worker.ts` owns only the permissioned VM context and JSON-lines bridge. `extensions/agent-browser/lib/process-environment.ts` owns async-local clearing of ambient upstream launch/proxy controls for every script-owned helper/cleanup subprocess. `extensions/agent-browser/lib/orchestration/script-mode.ts` owns strict lease parsing/appends, wrapper-verified spill rehydration, bounded compact result shaping, and cleanup recovery actions. `extensions/agent-browser/index.ts` owns full-executor recursion, unique isolated-session injection, durable pre-spawn lease timing, finally-close, branch/start recovery, and active-child shutdown reaping. Source-loaded Pi runs still execute `dist/extensions/agent-browser/script-worker.js`; run `npm run build` before direct TypeScript script-mode smoke tests. A missing compiled worker must return a structured setup failure, never throw from the tool.
- **Security invariants:** source never runs in the Pi host; no parent promise/object/function is exposed to source; `browser` and `emit` have null prototypes; IPC is JSON-only and byte-bounded; inner calls are parent-serialized; the session identity is wrapper-generated and never caller-selectable; restore is disabled; ambient `AGENT_BROWSER_*` and proxy controls are cleared during script planning/spawn/cleanup, with an empty private native config to bypass user/project profile defaults; every accepted first call records a strict Pi custom entry before upstream spawn; cleanup always runs and branch changes await it before restore; Pi `--no-session` rejects script before child/browser launch. Do not weaken these invariants, add host APIs/imports, or add identity/lifecycle/attachment/persistent-launch commands without a fresh security review.
- **No recipe layer:** script is ad hoc source for one call only. Do not add names, registries, persistence, imports, shared workflow state, or versioning under this surface; the no-recipe decision remains in [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md#no-reusable-recipe-layer-yet).
- **When changing behavior:** update [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#script), README, [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md#one-shot-code-mode), [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md#one-shot-script-isolation), [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0142`, prompt/playbook strings (`npm run docs -- playbook write`), `test/agent-browser.script.test.ts`, schema budget coverage, build/package expectations, and interactive tmux dogfood. Re-run a fresh-context security review after implementation changes.

### `semanticAction` shorthand (top-level tool input)

- **Compilation and validation:** `compileAgentBrowserSemanticAction` in `extensions/agent-browser/lib/input-modes/semantic-action.ts` turns the optional `semanticAction` object into upstream `find` argv; it is mutually exclusive with `script`, `args`, `job`, `qa`, `sourceLookup`, `networkSourceLookup`, and `electron` on the same call. Changing allowed actions, locators, or argv shape requires updating the schema beside that helper, the human contract in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#semanticaction), examples in [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md), and playbook strings in `extensions/agent-browser/lib/playbook.ts` (then `npm run docs -- playbook write`).
- **Optional upstream session field:** optional `semanticAction.session` compiles to a leading `--session <name>` before `find`. `buildExecutionPlan` in `extensions/agent-browser/lib/runtime.ts` then treats the planned argv like any call that already names an upstream session (it does not prepend the extension-managed implicit `--session` on top of that). Unified results echo `details.sessionName` for that name when present with `usedImplicitSession: false` (the call consumed upstream’s named session, not the extension-managed implicit slot); `retry-semantic-action-after-stale-ref` copies `compiledSemanticAction.args`, while `try-*-candidate` actions rebuild role/name fallback argv and preserve the same `--session` prefix.
- **Stale-ref machine follow-up:** for unified failures classified as `stale-ref` when `details.compiledSemanticAction` is present, `extensions/agent-browser/index.ts` appends `retry-semantic-action-after-stale-ref` to any `details.nextActions` from presentation (after `buildAgentBrowserNextActions` in `action-recommendations.ts`). Changing when that append runs, its payload shape, or its safety copy requires updating [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#semanticaction), [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), playbook text if guidance shifts, and `test/agent-browser.extension-validation.test.ts`.
- **Selector-not-found locator candidates:** for unified failures classified as `selector-not-found` when `details.compiledSemanticAction` is present, the same module appends `buildSemanticActionCandidateActions` results (ids prefixed with `try-`, each with redacted `params.args`) and prepends matching `Agent-browser candidate fallbacks` lines to the primary visible text via `formatSemanticActionCandidateText`. Raw `find` and semantic selector misses may also run one fresh `snapshot -i`, merge `details.visibleRefFallback`, append `try-current-visible-ref*` actions, and prepend `Current snapshot ref fallback` text when exact visible role/name refs exist. Changing which locator/action pairs produce candidates, argv shape, ids, snapshot probe gates, exact-match rules, or copy requires updating those helpers, [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#semanticaction), [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details), playbook strings when prompt guidance should change, and `test/agent-browser.extension-validation.test.ts`.
- **Regression tests:** `test/agent-browser.extension-input-modes.test.ts` asserts successful `semanticAction` compiled argv and optional `session` / `details.sessionName`, while `test/agent-browser.extension-validation.test.ts` covers pre-spawn validation errors, selector-not-found candidate fallbacks when the fake upstream reports a miss (including `--session` on candidate argv when `session` was set), plus stale-ref retry behavior when applicable.

### Electron desktop lifecycle (top-level `electron`)

- **Host modules:** `extensions/agent-browser/lib/electron/discovery.ts` (install scans and Electron evidence; `discoverElectronApps` scans macOS/Linux only—Windows returns `platform: "unsupported"` for `list`, while `inspectElectronExecutablePath` can still verify Windows binaries for `launch`), `cdp.ts` (bounded CDP `/json/version` and `/json/list` fetch/parse helpers plus `boundElectronProbeString`), `launch.ts` (isolated `userDataDir`, `--remote-debugging-port=0`, `DevToolsActivePort` polling, CDP metadata), `cleanup.ts` (tracked PID/port/session/profile teardown and status probes).
- **Orchestration modules:** `extensions/agent-browser/lib/input-modes/electron.ts` owns `compileAgentBrowserElectron`; `extensions/agent-browser/lib/orchestration/electron-host/index.ts` owns wrapper host actions (`list`, `status`, `probe`, `cleanup`), transcript restore for wrapper `launchId` records (`restoreElectronLaunchRecordsFromBranch`), and host-action result assembly. `extensions/agent-browser/lib/orchestration/browser-run/prepare.ts` / `final-result.ts` own `electron.launch` connect and handoff execution plus merged `details.electron*` / `details.compiledElectron`, post-command health checks, and structured next actions documented in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#electron). `extensions/agent-browser/index.ts` owns the runtime-vs-branch record scope, serialization with managed-session work, cleanup ownership sync, reload preservation of current branch-visible Electron launches plus their temp profiles, and off-branch cleanup; keep it limited to registration, session state initialization, cleanup dispatch, and tool-call routing.
- **When changing behavior:** Pi cancellation must stop `electron.launch` before host spawn or interrupt readiness polling and clean the process/profile if spawn already happened. Update [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#electron), [`docs/ELECTRON.md`](docs/ELECTRON.md) (public lifecycle and troubleshooting guide), [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md), [`README.md`](README.md) agent copy, [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0096`, playbook strings in `extensions/agent-browser/lib/playbook.ts` when operator guidance shifts (`npm run docs -- playbook write`), plus `test/agent-browser.extension-electron-discovery.test.ts`, `test/agent-browser.extension-electron-lifecycle.test.ts`, `test/agent-browser.extension-ref-guards.test.ts`, `test/agent-browser.extension-validation.test.ts`, `test/agent-browser.results.test.ts`.

### Constrained `job` (top-level tool input)

- **Compilation and validation:** `compileAgentBrowserJob` in `extensions/agent-browser/lib/input-modes/job.ts` turns the optional `job` object into upstream `batch` argv plus JSON-array stdin; it is mutually exclusive with `script`, `args`, `semanticAction`, `qa`, `sourceLookup`, `networkSourceLookup`, and `electron` on the same call and rejects caller-provided `stdin` alongside `job`. Changing allowed step actions, fields, or argv shape requires updating that helper and `AGENT_BROWSER_JOB_STEP_ACTIONS`, the TypeBox schema in `extensions/agent-browser/lib/input-modes/params.ts` (`AGENT_BROWSER_PARAMS`), the human contract in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#job), examples in [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) and [`README.md`](README.md), playbook strings in `extensions/agent-browser/lib/playbook.ts` (then `npm run docs -- playbook write`), and `test/agent-browser.extension-input-modes.test.ts` plus `test/agent-browser.extension-validation.test.ts`.
- **Runtime and presentation:** compiled jobs still pass through `buildExecutionPlan` like any other `args: ["batch", …]` call (implicit `--session`, `--json`, redacted `effectiveArgs`, batch validation). Result shaping uses the same `batch` / `batchSteps` presentation paths as hand-written batch stdin; there is no separate stale-ref retry append for `job` the way `semanticAction` gets `retry-semantic-action-after-stale-ref`, but the same pre-spawn page-scoped `@e…` snapshot guard applies to the generated `batch` stdin.
- **Regression tests:** `test/agent-browser.extension-input-modes.test.ts` covers successful compilation and redaction of secrets inside compiled stdin; `test/agent-browser.extension-validation.test.ts` covers mutual exclusion with `args` / `semanticAction` / `sourceLookup` / `networkSourceLookup` / caller `stdin`, and invalid steps.

### Lightweight `qa` preset (top-level tool input)

- **Compilation and validation:** `compileAgentBrowserQaPreset` in `extensions/agent-browser/lib/input-modes/job.ts` builds a fixed diagnostic `batch` plan (buffer clears, `open`, `wait --load <loadState>` (default `domcontentloaded`), optional text/selector waits, optional `network requests` / `console` / `errors` reads, optional `screenshot`) from the `qa` object; it is mutually exclusive with `script`, `args`, `semanticAction`, `job`, `sourceLookup`, `networkSourceLookup`, and `electron` on the same call and rejects caller-provided `stdin` alongside `qa`. Changing fields, default checks, or compiled argv requires updating that helper, the TypeBox schema in `extensions/agent-browser/lib/input-modes/params.ts` (`AGENT_BROWSER_PARAMS`), the human contract in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#qa), README and [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) examples, playbook strings in `extensions/agent-browser/lib/playbook.ts` (then `npm run docs -- playbook write`), and `test/agent-browser.extension-input-modes.test.ts` plus `test/agent-browser.extension-validation.test.ts`. Downstream QA pass/fail classification lives in `analyzeQaPresetResults` in the same module; failed-request impact splits use `classifyNetworkRequestFailure` / `summarizeNetworkFailures` in `extensions/agent-browser/lib/results/network.ts`. Extending failure semantics or benign-vs-actionable network rules requires updating those helpers, [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#qa) / [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#details) (`failureCategory: "qa-failure"`), [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) QA and network diagnostic notes when operator copy shifts, `extensions/agent-browser/lib/results/presentation/diagnostics.ts` when `network requests` summaries or per-row tags change, and extension-validation plus presentation test expectations.
- **Runtime and presentation:** compiled QA presets use the same `batch` path and presentation as `job` until `analyzeQaPresetResults` may reclassify a successful upstream batch as a tool failure when diagnostics report console errors, page errors, **actionable** failed network requests (per `classifyNetworkRequestFailure`), or any batch step failure. Rows classified as benign low-impact browser icon misses contribute to `qaPreset.warnings` instead of `failedChecks` and do not flip the preset to failed when nothing else is wrong.
- **Regression tests:** `test/agent-browser.extension-input-modes.test.ts` covers compilation and diagnostic failure classification; `test/agent-browser.extension-validation.test.ts` covers mutual exclusion with other input modes and caller `stdin`.

### Experimental `sourceLookup` (top-level tool input)

- **Compilation and validation:** `compileAgentBrowserSourceLookup` in `extensions/agent-browser/lib/input-modes/lookups.ts` turns the optional `sourceLookup` object into upstream `batch` argv plus JSON-array stdin (steps such as `is visible`, `get html`, `react inspect`, `react tree` depending on which of `selector`, `reactFiberId`, and `componentName` are set). It is mutually exclusive with `script`, `args`, `semanticAction`, `job`, `qa`, `networkSourceLookup`, and `electron` on the same call and rejects caller-provided `stdin` alongside `sourceLookup`. Changing fields, compiled steps, or analysis rules requires updating that helper, `analyzeSourceLookupResults` in the same module, the TypeBox schema in `extensions/agent-browser/lib/input-modes/params.ts` (`AGENT_BROWSER_PARAMS`), the human contract in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#sourcelookup), examples in [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) and [`README.md`](README.md), playbook strings in `extensions/agent-browser/lib/playbook.ts` (then `npm run docs -- playbook write`), and `test/agent-browser.extension-validation.test.ts`.
- **Runtime and presentation:** compiled source lookups pass through `buildExecutionPlan` like any other `args: ["batch", …]` call. After the upstream invocation returns, `analyzeSourceLookupResults` walks batch JSON, optional selector HTML, and the Pi session `cwd` workspace scan (when `componentName` is present), merges `details.sourceLookup`, and prepends a short human summary to visible text when present; it does **not** apply `qa`-style reclassification that marks the tool failed on an upstream-successful batch solely because diagnostics look bad—failed batch steps still surface as normal tool errors. `details.sourceLookup.status` is only `unsupported` when no candidates were collected and a `react` batch step failed; any DOM or workspace candidate keeps `candidates-found` even if React inspection failed.
- **Scan bounds:** workspace walks honor `maxWorkspaceFiles` (default 2000, hard cap 5000), only consider `.ts`/`.tsx`/`.js`/`.jsx` under the session cwd while skipping dirs such as `node_modules` and `dist`, record at most ten `workspace-search` candidates, and omit the selector `get html` step when `includeDomHints` is `false`. `details.sourceLookup.limitations` always opens with the experimental disclaimer strings and may append a notice when the file walk hits the configured cap.
- **Regression tests:** `test/agent-browser.extension-validation.test.ts` covers compilation, `details.sourceLookup` / `details.compiledSourceLookup`, mutual exclusion with other input modes and caller `stdin`, and validation errors for empty inputs and `maxWorkspaceFiles` bounds.

### Experimental `networkSourceLookup` (top-level tool input)

- **Compilation and validation:** `compileAgentBrowserNetworkSourceLookup` in `extensions/agent-browser/lib/input-modes/lookups.ts` turns the optional `networkSourceLookup` object into upstream `batch` argv plus JSON-array stdin (`network request <id>` when `requestId` is set, plus `network requests --filter …` when `filter` or `url` is set—the latter supplies the filter pattern when `filter` is omitted). It is mutually exclusive with `script`, `args`, `semanticAction`, `job`, `qa`, `sourceLookup`, and `electron` on the same call and rejects caller-provided `stdin` alongside `networkSourceLookup`. At least one of `requestId`, `filter`, or `url` is required. Changing fields, compiled steps, or analysis rules requires updating that helper, `analyzeNetworkSourceLookupResults` in the same module, the TypeBox schema in `extensions/agent-browser/lib/input-modes/params.ts` (`AGENT_BROWSER_PARAMS`), the human contract in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md#networksourcelookup), examples in [`docs/COMMAND_REFERENCE.md`](docs/COMMAND_REFERENCE.md) and [`README.md`](README.md), playbook strings in `extensions/agent-browser/lib/playbook.ts` (then `npm run docs -- playbook write`), and `test/agent-browser.extension-validation.test.ts`.
- **Runtime and presentation:** compiled network source lookups pass through `buildExecutionPlan` like any other `args: ["batch", …]` call. After the upstream invocation returns, `analyzeNetworkSourceLookupResults` walks batch JSON for failed requests (HTTP status ≥ 400, `failed: true`, or an `error` field), collects initiator-like metadata into `details.networkSourceLookup.candidates`, optionally scans the Pi session cwd for URL/path string literals (same file-type and skip-dir rules as `sourceLookup`), merges `details.networkSourceLookup`, and runs URL redaction on echoed URLs, evidence strings, and `compiledNetworkSourceLookup` (`redactNetworkSourceLookupUrl` / `redactNetworkSourceLookupAnalysis` in `extensions/agent-browser/index.ts`). It does **not** reclassify an upstream-successful batch to failed when no candidates are found; failed batch steps still surface as normal tool errors.
- **Scan bounds:** workspace URL literal search uses the same `maxWorkspaceFiles` default and cap as `sourceLookup`, derives up to eight search needles from the query and failed request URLs, and records at most ten `workspace-search` candidates before stopping.
- **Regression tests:** `test/agent-browser.extension-validation.test.ts` covers compilation, redaction of secrets in compiled and analysis surfaces, `details.networkSourceLookup` / `details.compiledNetworkSourceLookup`, mutual exclusion with other input modes and caller `stdin`, and validation errors for empty inputs.

### Runtime planning (`extensions/agent-browser/lib/runtime.ts`)

Native configured session/namespace selection is adapted at `lib/orchestration/native-session-defaults.ts` before the existing caller-owned planning/queue path. Read only identity fields and use sessionless native config validation rather than duplicating its schema; carry explicit config/idle overrides through the async-local process environment. Keep no-default implicit ownership unchanged. `test/agent-browser.shared-defaults.test.ts` covers caller ownership, precedence, helpers and the opt-in local-profile/script boundary.

This module is **pre-spawn planning only**: it validates raw `args`, derives implicit or fresh managed session names, classifies browser prompts, redacts invocation text, and builds `effectiveArgs` for the upstream binary. It does not spawn processes or touch the filesystem.

- **`buildExecutionPlan`** — Injects `--json`, optionally injects extension-managed `--session` for `sessionMode: "auto"`, applies the site-specific headless `--user-agent` workaround when eligible by argv and upstream launch environment, retains it for the current wrapper-managed session while preparation restores per-owned-session compatibility for off-current targets, and returns `validationError` plus a structured `recoveryHint` when launch-scoped flags would be ignored on an already-active implicit session (surfaced to Pi as `details.sessionRecoveryHint` after redaction in `extensions/agent-browser/index.ts`).
- **Command discovery** — `extensions/agent-browser/lib/argv-descriptor.ts` and `extensions/agent-browser/lib/argv-grammar.ts` own `findCommandStartIndex`, `VALUE_FLAGS`, `parseArgvDescriptor`, and `parseCommandInfo`; `buildExecutionPlan` consumes the descriptor instead of re-parsing tokens. `findCommandStartIndex` walks leading tokens using `VALUE_FLAGS` and `GLOBAL_BOOLEAN_FLAGS_WITH_OPTIONAL_VALUES` so known value-taking flags ahead of the upstream command word consume their values before the command is identified. `stripUpstreamGlobalFlags` / `extractUpstreamCommandTokens` then mirror upstream 0.34.0 `clean_args` across the full command tail for semantic, lifecycle, presentation, and protected-path parsing while `extractCommandTokens` remains the raw-slice helper for argv reconstruction. Artifact consumers clean globals only on outer CLI argv; native `batch` rows go directly to upstream `parse_command`, so global-looking row tokens remain literal operands. Missing-value prevalidation is narrower: `PREVALIDATED_VALUE_FLAGS` is global-value-only so command-scoped flags or literal free text such as `fill #field --password` are left to upstream parsing instead of being rejected by wrapper heuristics. A smaller set of global booleans may be followed by an optional `true`/`false` literal; when present, that literal is consumed before command discovery continues, and repeated boolean flags follow upstream's last-occurrence-wins semantics with only a separated exact lowercase `false` disabling the final recognized occurrence; unsupported equals tokens such as `--auto-connect=false` are rejected before dispatch instead of overriding an earlier bare flag. When upstream adds new globals that take a separate argv value before the subcommand, extend command discovery and prevalidation; for new command-specific flags, extend command discovery/redaction only when wrapper features need to understand them.
- **Launch-scoped flags** — `extensions/agent-browser/lib/launch-scoped-flags.ts` owns `LAUNCH_SCOPED_FLAG_DEFINITIONS` (currently `--allowed-domains`, `--auto-connect`, `--namespace`, `--cdp`, `--enable`, `--executable-path`, `--webgpu`, `--no-webmcp`, `--init-script`, `--idle-timeout`, `--args`, `--user-agent`, `--headed`, `--device`, `--profile`, `--provider`, `-p`, `--session-name`, `--restore`, `--restore-save`, restore check flags, and `--state`); `getStartupScopedFlags` mirrors that metadata. `--auto-connect` counts only when enabled (bare or `true`); `--state` after a `wait` command is treated as a wait predicate, not launch state, so it does not force `sessionMode: "fresh"` during implicit reuse. Preparation also rejects launch-scoped flags for off-current wrapper-owned sessions after daemon inspection proves the target active. Keep `--allowed-domains` fresh-launch-only: upstream owns network/worker/popup/WebRTC containment and incompatible-mode rejection; do not add wrapper enforcement.
- **Tab-correction signal** — `hasLaunchScopedTabCorrectionFlag` is a subset (`--profile`, `--session-name`, `--state`) for post-open tab re-selection when restored tabs steal focus; it intentionally excludes `--cdp`, `--auto-connect`, and provider selection flags (`-p`, `--provider`, `--device`).
- **Process identity probes** — `extensions/agent-browser/lib/process-identity.ts` centralizes POSIX `ps` and native-Windows PowerShell command construction for PID-reuse-safe daemon-policy locks and secure-temp stale-root cleanup.
- **Upstream subprocess environment** — `extensions/agent-browser/lib/process.ts` passes through the parent environment so user-approved provider credentials and other runtime variables reach upstream, then applies wrapper overrides such as the managed socket directory, managed-session idle timeout, clamped default operation timeout, canonical owned-session namespace (including empty default), and managed-session restore env from the pure `getManagedSessionRestoreEnv` resolver in `extensions/agent-browser/lib/managed-session-restore.ts`; on POSIX, the selected socket directory must be absolute, current-uid-owned mode `0700`, under trusted ancestry, and free of symlink, foreign-owner, or special planted entries, with pre-existing unsafe modes rejected rather than repaired and validation repeated before spawn; `commitManagedSessionRestoreSuppression` records a restore-disabled identity only after an owned-context child emits `spawn` on POSIX; the Windows `cross-spawn` path commits after process completion unless the spawner reports an error such as `ENOENT`. Every subprocess targeting a wrapper-owned session—including helper probes such as fresh snapshots, tab lists, and navigation summaries—must receive the same implicit `AGENT_BROWSER_IDLE_TIMEOUT_MS`; caller-owned sessions retain native idle policy (including an unset value), and per-call idle/config overrides must reach their helpers; compatible non-close subprocesses must also receive the same restore decision for a given managed session, while close omits the wrapper's automatic key without changing caller environment. Wrapper-owned headed launches default `AGENT_BROWSER_AUTOSAVE_INTERVAL_MS` to `0` unless the caller set it because upstream 0.33.2's periodic multi-origin collector opens visible temporary targets and holds the daemon lock; the owned managed-session context retains the effective launch-time interval across every helper and follow-up subprocess, including transcript reload/resume, so daemon configuration stays stable; any explicit change in either direction must fail until close plus a fresh launch. Native close still saves, but direct window close can lose newer state because headed browsers are exempt from idle shutdown. Upstream can otherwise restart the background browser between calls, discard the active tab, invalidate fresh refs, or switch restore keys mid-session. Keep model-facing output redacted, but do not drop execution-path secrets merely because their names are not known to this wrapper.
- **Attached browser continuity** — successful `connect`, `--cdp`, enabled `--auto-connect`, matching environment launches, and wrapper Electron attachments are branch-visible session state. First-use and later content-bearing calls live-check `get url` so external tab drift cannot silently target the wrong page. Caller launch/config/file-access arguments and environment remain upstream-owned and pass through unchanged; the wrapper only avoids re-emitting its own compatibility launch argument on an active attachment. A successful close clears the attachment marker. Update `docs/ARCHITECTURE.md`, `docs/TOOL_CONTRACT.md`, `docs/COMMAND_REFERENCE.md`, plus process and extension integration tests when changing this boundary.
- **Managed-session restore ownership** — automatic managed restore is a wrapper convenience, not an authorization boundary. Wrapper-owned implicit sessions receive a transcript- and checkout-scoped restore identity when compatible; explicit caller sessions, state/restore choices, profiles, config, file access, launch arguments, environment, and close arguments pass through unchanged. Do not reserve `piab-*` names, hide session/state rows, redact restore identifiers, pin an empty upstream config, clear caller launch environment, canonicalize caller close arguments, or reject local/cross-checkout paths. Keep only the automatic restore identity/storage checks needed to avoid mixing the wrapper's own restore pools, the daemon lifecycle coordination needed to avoid corrupting active managed sessions, and live page-target verification after ambiguous tab/attachment/history/script/state transitions. `extensions/agent-browser/lib/page-target-validation.ts` owns page-target correctness only; it must not grow resource-access policy. Update [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md), [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md), `test/agent-browser.page-target-validation.test.ts`, managed-restore tests, planner coverage, and browser-run call sites together.
- **Upstream subprocess completion** — `runAgentBrowserProcess` spawns `agent-browser` with `stdio: ["pipe", "pipe", "pipe"]` and must not wait forever on `"close"` when a detached descendant inherits those streams. `watchSpawnedChildCompletion` listens for `exit` and `close`, leaves piped stdio intact for the short post-`exit` grace (`EXIT_STDIO_GRACE_MS`) so normal `close` can still win, then destroys those streams only if the fallback resolves with the observed `exit` code. Windows invocations use the public `cross-spawn.spawn` API without custom quoting, executable discovery or command reordering; POSIX keeps native Node `spawn`. Timeout/abort cleanup uses `taskkill /T /F` for the full Windows shell/agent-browser process tree before falling back to the direct child signal. `test/agent-browser.windows-argv.test.ts` runs the actual host transport with independent argv, stdin, PATH-shim and failure controls, including in `platform-target`; POSIX results are not native Windows proof. Exit-code precedence is `close` → wrapper timeout (`124`) → post-`exit` fallback → spawn failure (`127`), locked by `resolveSpawnedChildExitCode` and `test/agent-browser.process.test.ts`. Changing that window, precedence, or stream teardown order requires updating [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) subprocess bullets, [`docs/SUPPORT_MATRIX.md`](docs/SUPPORT_MATRIX.md) `RQ-0097`, and the stdio-linger regressions in `test/agent-browser.process.test.ts`.
- **Sessionless inspection/local commands** — `isPlainTextInspectionArgs` recognizes `--help`, `-h`, `--version`, and `-V`: those calls keep caller argv, skip implicit `--session`, and are ignored during transcript restore. `needsManagedSession` in `command-policy.ts` gives the same no-implicit-session behavior to read-only skills, local auth/profile/setup commands, `session list`, and syntactically local state management. State/session rows and targets are not filtered or reserved. Browser-backed or context-dependent commands still receive normal managed-session injection when the caller did not choose an explicit session. Cover command-shape changes in `test/agent-browser.runtime.test.ts`.
- **Regression tests** — `test/agent-browser.runtime.test.ts` locks execution-plan injection, launch-scoped blocking, argv edge cases, and related helpers.

Product-facing behavior and rationale live in [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) under **Session model** / **Launch flags**; agent-visible fields live in [`docs/TOOL_CONTRACT.md`](docs/TOOL_CONTRACT.md) (`sessionMode`, `sessionRecoveryHint`, `managedSessionRestoreDisabled`, `effectiveArgs`).

### Maintainer rebaselining workflow

Use this sequence when upstream ships a new `agent-browser` version or help text changes enough to break the live verifier:

1. Capture real `agent-browser … --help` output from the binary you intend to target, then edit `scripts/agent-browser-target.mjs` for the runtime version and `scripts/agent-browser-capability-baseline.mjs` for help/doc inventory. Both files are import-only metadata and never shell out to `agent-browser`, so rebaselining stays an explicit maintainer decision.
2. Keep `docs/COMMAND_REFERENCE.md` human prose aligned with the baseline: every inventory token the baseline expects in the doc must appear **outside** the two generated regions (`upstream-baseline`, `capability-token-baseline`). `scripts/verify-command-reference.mjs` strips those generated blocks and fails if any required token is missing from the remaining Markdown.
3. Regenerate the HTML-comment bounded blocks with `npm run docs -- command-reference write`. If you also changed playbook source in `extensions/agent-browser/lib/playbook.ts`, refresh every generated doc fragment in one shot with `npm run docs -- write` (runs playbook drift rewrite plus command-reference rewrite).
4. Before committing, run `npm run docs` (or `npm run docs -- command-reference check`) so checked-in blocks cannot drift from the baseline file.
5. When the targeted `agent-browser` is available on `PATH`, run `npm run verify -- command-reference`. It enforces `agent-browser --version` against the baseline, re-samples each `helpCommands` invocation for expected upstream tokens, and repeats the human-token scan against `docs/COMMAND_REFERENCE.md`.

## Preferred testing workflow

Use an end-to-end interactive `pi` run inside `tmux`.

### Rules

- Use two distinct validation modes and do not mix their assumptions:
  - **Checkout-only extension mode:** launch `pi --approve --no-extensions -e .` from this trusted repository root. Automatic extension loading is disabled, but Pi settings, configured package resolution, and other resource types remain active. For isolated test settings, use temporary `HOME` and `PI_CODING_AGENT_DIR` directories with `PI_OFFLINE=1`; provide only the model credentials the smoke needs. Restart Pi after extension edits rather than using `/reload` as this mode's validation target. Omit `--approve` only when testing Project Trust.
  - **Configured-source lifecycle mode:** run `npm run verify -- lifecycle` before every publish for deterministic `/reload`, exact `--session-id` relaunch, managed-session continuity, persisted-spill, and Pi failure-patch regression coverage (see [`docs/RELEASE.md`](docs/RELEASE.md#pre-release-checks)). Focused extension harness tests cover Pi `session_tree` branch rehydration and cleanup ownership. The lifecycle harness defaults to Pi model `zai/glm-5.2`, **180000 ms** per-step waits in `scripts/verify-lifecycle.mjs`, and accepts `--model <id>` / `--timeout-ms <ms>` passthrough. The npm verification facade removes npm-injected `node_modules/.bin` entries from the lifecycle child `PATH` so tmux launches the maintainer's host Pi instead of a development dependency binary (for example `npm run verify -- lifecycle --model openai-codex/gpt-5.5:minimal`). During development you can also configure exactly one active source for this extension in Pi settings and launch plain `pi` for manual validation of reload/relaunch behaviors because `/reload` exercises auto-discovered/configured resources.
- For code changes, a checkout-only `pi --approve --no-extensions -e .` smoke session is a pre-commit requirement; whoever made the changes should validate unpublished checkout behavior there before commit/push (a published install alone is not a substitute).
- When validating real extension behavior, run real Pi/model sessions with the target model, for example `pi --approve --model zai/glm-5.2:high --no-extensions -e ./extensions/agent-browser/index.ts`. Do **not** add `--no-session` to these checks: preserve the Pi JSONL transcript so tool-result semantics, model-visible content, and follow-up behavior can be reviewed. Prefer a disposable `--session-dir <temp-dir>` when isolation is needed, then inspect the saved session JSONL and clean up the temp session directory after recording evidence.
- Before every release, run real-site interactive dogfood in `tmux` with `pi --approve --no-extensions -e .` and the native `agent_browser` tool. Cover at least one simple static site and one real docs/product site, include `qa` or `job`/`batch` when those wrapper surfaces changed, verify refs/navigation/artifacts/session cleanup from the transcript, close sessions, remove screenshots/temp files, and record the outcome in release evidence. Automated localhost, fake-upstream, and package smoke gates are not a substitute for this live-site tmux dogfood. When a smoke prompt is meant to validate this extension rather than external dogfood/QA skill behavior, add `--no-skills` and use the bounded release-smoke wording in [`docs/RELEASE.md`](docs/RELEASE.md#public-sauce-demo-checkout-smoke-prompt); run skill-enabled dogfood separately only when skill routing itself is under test. When dashboard, diagnostic, artifact, recording, scroll, or combobox behavior changed, use the public Grafana checklist in [`docs/RELEASE.md`](docs/RELEASE.md#public-grafana-stress-checklist); do not commit private dogfood/VFR skills or run artifacts.
- For installed-package validation after publish: update the real installed package, then exercise it with only the published package active—either temporarily disable/remove the checkout path and run plain `pi` for lifecycle validation, or use an ephemeral extension-only smoke run like `pi --no-extensions -e npm:pi-agent-browser-native@<version>` (with the isolated settings environment above when needed).
- Use `tmux` via bash commands.
- Do **not** use the pi interactive shell extension for this workflow.
- Drive `pi` like a real user by sending prompts normally.
- The extension blocks direct `agent-browser` bash calls by default to push agents toward the native tool. During this package's own development, the guard is bypassed when the session cwd is this package root (`package.json` name `pi-agent-browser-native`) or when `PI_AGENT_BROWSER_ALLOW_DIRECT_BASH=1` is set, so upstream CLI behavior can be debugged directly.
- After extension changes in configured-source lifecycle mode, `/reload` is the minimum, but a full close-and-relaunch of `pi` is preferred for higher confidence. The automated `npm run verify -- lifecycle` harness creates an isolated `PI_CODING_AGENT_DIR`, configures a temporary package source, drives `pi --approve` in `tmux` with an exact `--session-id`, uses a deterministic fake upstream `agent-browser`, checks `/reload` plus restart with the same session id, and captures transcript artifacts on failure.
- If continuing the same conversation after restart, use `/resume` or an explicit session path/id.
- Resumed/exact-session sessions should reflect the updated configured extension source after restart.

### Practical tmux notes

- Prefer `tmux send-keys ... Enter` for prompt submission. Collapse multi-line smoke prompts to one line before `send-keys`, or use Pi's external editor / tmux extended-keys path; raw pasted newlines become separate queued user messages.
- Capture larger pane ranges when debugging: `tmux capture-pane -p -S -300 -t <session>:0.0`.
- Clean up tmux sessions after testing.
- Before ending a turn, sweep for and remove repo-local scratch files, project-scoped temp artifacts, and lingering browser sessions created during the work unless the user explicitly asked to keep them.
- Do not overfit testing to `example.com`; use it for smoke checks only, then validate against additional realistic pages and flows. The lifecycle harness intentionally uses a fake upstream browser for deterministic lifecycle assertions and does not replace occasional real-browser manual smoke testing before release.
- Prefer **tmux** over `pi -p` for real-browser checkout and release smokes; print mode may hang without a TTY. Use npm 11 when touching `package-lock.json` (`packageManager` in `package.json`), and never commit WorkOS or other private-registry URLs in that lockfile. After real-browser runs, close sessions and sweep lingering browser/tmux temp artifacts. Maintainer environment pitfalls (Pi/npm version skew, lifecycle `/reload` timing, automated pane matchers) are documented in [`docs/RELEASE.md`](docs/RELEASE.md#environment-and-automation-pitfalls).

## Current testing focus

Before calling the first implementation usable, verify at minimum:

- missing-`agent-browser` error path is clear
- native tool invocation works from interactive `pi`
- `open` works
- `snapshot -i` works
- `click` works
- `batch` via stdin works
- `eval --stdin` works
- screenshot attachment works
- implicit session reuse works
- explicit `--session` passthrough works
- configured-source `/reload` preserves runtime continuity
- full restart + exact `--session-id` relaunch or `/resume` picks up changed compiled extension code
- when `electron.*`, attached-session diagnostics, or `qa.attached` change: exercise `electron.list` → `electron.launch` → browser commands → `electron.cleanup` on a local Electron app (see [`docs/RELEASE.md`](docs/RELEASE.md) and [`docs/ELECTRON.md`](docs/ELECTRON.md))

# Support decision index

## Purpose

This is a compact index from closed requirement IDs to the current contract or decision. It is not a historical work log. Current release status and verification gates live in [`SUPPORT_MATRIX.md`](SUPPORT_MATRIX.md); canonical ownership is mapped in [`SOURCE_OF_TRUTH.md`](SOURCE_OF_TRUTH.md).

| Requirement | Current decision | Canonical source |
| --- | --- | --- |
| RQ-0066 | `sourceLookup` is an experimental, bounded candidate finder compiled to upstream batch steps; missing candidates do not turn an otherwise successful batch into failure. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#sourcelookup), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md) |
| RQ-0067 | `networkSourceLookup` correlates failed requests with initiator data and workspace URL literals, redacts sensitive URLs, and reports candidates rather than blame. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#networksourcelookup), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md) |
| RQ-0068 | No reusable browser-recipe runtime. Revisit only with repeated failure-prone sequences that `job`, `qa`, or raw `batch` cannot represent. | [`ARCHITECTURE.md`](ARCHITECTURE.md#no-reusable-recipe-layer-yet), [`ELECTRON.md`](ELECTRON.md) |
| RQ-0070–RQ-0071 | Failed semantic locators may expose bounded role/name fallbacks; optional `semanticAction.session` and all generated retries preserve the named upstream session. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#semanticaction), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md#selector-strategy) |
| RQ-0072 | Refs are page-scoped, restored from the active transcript branch, invalidated by page-changing work (including conservative invalidation after any executed `record start` attempt and URL-bearing `record restart` for older-native protection, not proof of a page change; FPS-only restarts keep refs), invalidated with a persisted summary after failed non-batch transition commands (`eval`/`back`/`forward`/`reload`/`connect`/`state load`/`tab`) whose live URL probe keeps the page verified, and rejected before spawn when stale or absent; upstream ref-resolving reads and captures (`is`, `screenshot`, `highlight`, `scroll`, `frame`, `diff`) are guarded like mutations, while `wait`, `a11y`, and `find` stay unguarded because upstream treats their operands as literal selectors, text, or locator values, and values of value-taking flags other than `--selector`/`-s` (for example `--text` or `--baseline`) are read as literals while refs after boolean flags (for example `click --new-tab @e1`) stay guarded; batch scanning uses upstream's exclusive step source (raw batch argument strings when present — filtering only the exact `--bail` token — stdin otherwise), so raw-argv batch refs and raw-argv `record start` latching are guarded too, and tab-pinned batch rewrites dispatch those same effective steps (re-emitting the caller's exact `--bail` token) instead of resurrecting ignored stdin. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`ARCHITECTURE.md`](ARCHITECTURE.md) |
| RQ-0073–RQ-0074 | Surface likely overlays and ambiguous/hidden selector text with read-only evidence and bounded follow-ups; do not invent or replay user actions. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md#selector-strategy) |
| RQ-0075–RQ-0079 | Classify actionable versus benign network failures, report timeout/session/getter recovery, align Pi failure rows, and keep explicit artifact cleanup host-owned. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#qa), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md) |
| RQ-0080–RQ-0083 | Report no-op scrolls, focused combobox recovery, and missing recording dependencies; keep public Grafana release checks in docs instead of bundling private dogfood workflows. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`RELEASE.md`](RELEASE.md#public-grafana-stress-checklist) |
| RQ-0086–RQ-0089 | Limit tab probes to observed drift, permit safe same-snapshot form work, recover exact current refs, and fail direct clicks when bounded dispatch evidence shows no native event. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md) |
| RQ-0090–RQ-0095 | Guard only machine-checkable requested artifacts before close; keep release smoke independent of external skills; support native select; prevent network URLs from replacing page state; expose bounded network follow-ups. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md), [`RELEASE.md`](RELEASE.md), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md) |
| RQ-0096 | First-class Electron support is a narrow typed lifecycle, not a generic recipe layer: wrapper-owned discovery, isolated launch, attach/handoff, status, probe, and cleanup; `qa.attached` reuses QA for attached sessions. Manual debug-port targets remain host-owned. | [`ELECTRON.md`](ELECTRON.md), [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#electron) |
| RQ-0097 | Subprocess completion cannot hang on descendant-owned stdio; raw CDP/no-active-page recovery offers safe tab inspection without making manually launched apps wrapper-owned. | [`ARCHITECTURE.md`](ARCHITECTURE.md#direct-subprocess-execution), [`ELECTRON.md`](ELECTRON.md#manual-host-launch-pattern) |
| RQ-0098 | Desktop readiness prefers condition waits, stable tab selection, `electron.probe`, and `qa.attached`; fixed sleeps are a last resort and never completion proof. | [`ELECTRON.md`](ELECTRON.md#readiness-and-waits), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md#wait-for-page-readiness-or-downloads) |
| RQ-0099 | Rich-input recovery resolves one exact current editable ref or returns focus/click follow-ups without copying text or submitting. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md#selector-strategy) |
| RQ-0100 | Known about-blank/tab drift produces machine-readable tab-list, select, and snapshot recovery without routine tab probes. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`ELECTRON.md`](ELECTRON.md#troubleshooting) |
| RQ-0101 | Dense compact snapshots keep omitted high-value controls discoverable through bounded summaries and `highValueControlRefIds`; named action links (row/navigation and repository-style result links) are first-class high-value candidates alongside editables, named surfaces, and primary action buttons. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md#snapshot) |
| RQ-0110–RQ-0120 | Headed-window visibility, loopback reachability, and fixture-server ownership remain environment/upstream concerns; the wrapper documents evidence limits and adds only narrow diagnostics for eval, clicks, selectors, sessions, and stdin misuse. | [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md), [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md) |
| RQ-0121 | Package config uses Pi-scoped layered files, a validated default Exa search type, and lazy/redacted credential sources; optional Exa/Brave search registration follows the canonical config policy. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#optional-companion-web-search), [`ARCHITECTURE.md`](ARCHITECTURE.md), `extensions/agent-browser/lib/config-policy.js` |
| RQ-0122 | Web searches are serialized, expose bounded Exa filters with provider preflight, and stop on provider rate limits; profile/executable failures lead to diagnostics rather than repeated opens. | [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md), [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#optional-companion-web-search) |
| RQ-0123–RQ-0127 | Stress findings hardened artifact verification, domain containment, QA/job failure behavior, semantic/ref recovery, timeout/dialog guidance, network diagnostics, and snapshot evidence; prompt-derived business-action blocking remains out of scope. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md), [`ARCHITECTURE.md`](ARCHITECTURE.md) |
| RQ-0128–RQ-0130 | Historical upstream rebaselines updated the capability baseline/docs/tests and removed obsolete workarounds without adding older-version compatibility shims. Later and current baselines are tracked in the active matrix. | `scripts/agent-browser-capability-baseline.mjs`, [`SUPPORT_MATRIX.md`](SUPPORT_MATRIX.md), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md) |
| RQ-0136 | All upstream subprocesses share the managed idle-timeout launch environment; `tab close` refreshes the remaining active target; document-direction scrolls run deterministically before wheel fallback. | [`ARCHITECTURE.md`](ARCHITECTURE.md), [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#details), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md#core-page-and-element-commands) |
| RQ-0142 | One-shot `script` handles bounded loops, conditional page branches, and multi-page aggregation in a permissioned child with unique always-closed browser isolation and durable cleanup leases; it deliberately does not add a named reusable recipe layer. | [`TOOL_CONTRACT.md`](TOOL_CONTRACT.md#script), [`ARCHITECTURE.md`](ARCHITECTURE.md#one-shot-script-isolation), [`COMMAND_REFERENCE.md`](COMMAND_REFERENCE.md#one-shot-code-mode) |
